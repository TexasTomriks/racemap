# racemap runtime dependencies
click>=8.1
pydantic>=2.6
rich>=13.7
requests>=2.31        # Ollama HTTP client
semgrep>=1.50          # static analysis engine (installed in the Docker image)

# Optional cloud triage backends (only used when the matching API key is set)
anthropic>=0.34            # --llm anthropic   (ANTHROPIC_API_KEY)
openai>=1.0                # --llm openai      (OPENAI_API_KEY)
google-genai>=1.0           # --llm gemini      (GEMINI_API_KEY)

# Web UI — Flask (web/server.py + web/templates), the UI used in the demo video
flask>=3.0

# Alternate Web UI — Streamlit (src/ui/app.py), not used in the demo video
streamlit>=1.30
pandas>=2.0
plotly>=5.18
streamlit-antd-components>=0.3

# Dev / test
pytest>=8.0

# External tools (NOT pip-installable — install on the Ubuntu analysis machine):
#   coccinelle  (provides `spatch`)   apt install coccinelle
#   ollama                             https://ollama.com    (then: ollama pull llama3.1)
